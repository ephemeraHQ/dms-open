/**
 * Where message backups should be stored
 */
export declare enum BackupType {
    none = 0,
    xmtpTopicStore = 1
}
export interface BackupProvider {
    type: BackupType;
}
export declare type SelectBackupProvider = () => Promise<BackupProvider>;
export interface NoBackupConfiguration {
    type: BackupType.none;
    version: number;
}
export interface TopicStoreBackupConfiguration {
    type: BackupType.xmtpTopicStore;
    version: number;
    topic: string;
    secret?: Uint8Array;
}
export declare type BackupConfiguration = NoBackupConfiguration | TopicStoreBackupConfiguration;
export default interface BackupClient {
    get backupType(): BackupType;
}
