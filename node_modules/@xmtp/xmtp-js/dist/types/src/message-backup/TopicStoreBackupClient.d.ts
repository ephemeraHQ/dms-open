import BackupClient, { BackupType, TopicStoreBackupConfiguration } from './BackupClient';
export default class TopicStoreBackupClient implements BackupClient {
    private configuration;
    static createConfiguration(walletAddress: string): TopicStoreBackupConfiguration;
    constructor(configuration: TopicStoreBackupConfiguration);
    get backupType(): BackupType;
}
