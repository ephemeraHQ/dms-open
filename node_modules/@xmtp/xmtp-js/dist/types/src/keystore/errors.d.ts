import { keystore } from '@xmtp/proto';
export declare class KeystoreError extends Error implements keystore.KeystoreError {
    code: keystore.ErrorCode;
    constructor(code: keystore.ErrorCode, message: string);
}
