"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.encryptV2 = exports.decryptV2 = exports.encryptV1 = exports.decryptV1 = void 0;
const crypto_1 = require("../crypto");
const decryptV1 = (myKeys, peerKeys, ciphertext, headerBytes, isSender) => __awaiter(void 0, void 0, void 0, function* () {
    const secret = yield myKeys.sharedSecret(peerKeys, myKeys.getCurrentPreKey().publicKey, // assumes that the current preKey is what was used to encrypt
    !isSender);
    return (0, crypto_1.decrypt)(ciphertext, secret, headerBytes);
});
exports.decryptV1 = decryptV1;
const encryptV1 = (keys, recipient, message, headerBytes) => __awaiter(void 0, void 0, void 0, function* () {
    const secret = yield keys.sharedSecret(recipient, keys.getCurrentPreKey().publicKey, false // assumes that the sender is the party doing the encrypting
    );
    return (0, crypto_1.encrypt)(message, secret, headerBytes);
});
exports.encryptV1 = encryptV1;
const decryptV2 = (ciphertext, secret, headerBytes) => (0, crypto_1.decrypt)(ciphertext, secret, headerBytes);
exports.decryptV2 = decryptV2;
const encryptV2 = (payload, secret, headerBytes) => (0, crypto_1.encrypt)(payload, secret, headerBytes);
exports.encryptV2 = encryptV2;
//# sourceMappingURL=encryption.js.map